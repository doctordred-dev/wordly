# 🚀 Быстрый старт Wordly

## Что это?
Приложение для изучения слов с автоматическим переводом, работает как Quizlet.

## Основные возможности
✅ Массовый ввод слов (вставьте сразу много слов)
✅ Автоматический перевод (бесплатный API MyMemory)
✅ Карточки с анимацией переворота
✅ База данных Supabase
✅ Современный UI с TailwindCSS

## Шаги для запуска

### 1. Настройка Supabase (5 минут)

1. Перейдите на [supabase.com](https://supabase.com)
2. Создайте аккаунт или войдите
3. Нажмите "New Project"
4. Заполните:
   - **Name**: wordly
   - **Database Password**: придумайте пароль
   - **Region**: выберите ближайший регион
5. Подождите 1-2 минуты пока проект создастся

### 2. Получите API ключи

1. В панели Supabase: **Settings** → **API**
2. Скопируйте:
   - **Project URL**
   - **anon public** key

### 3. Настройте .env.local

Откройте файл `.env.local` в корне проекта и вставьте ваши ключи:

```
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-ключ-здесь
```

### 4. Создайте таблицу в базе данных

1. В Supabase: **SQL Editor**
2. Нажмите "New Query"
3. Скопируйте содержимое файла `supabase-migration.sql`
4. Вставьте в редактор
5. Нажмите "Run"

### 5. Запустите приложение

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## Как использовать

### Добавление слов
1. Выберите языки (откуда → куда)
2. Введите слова, **по одному на строку**:
   ```
   hello
   world
   computer
   programming
   ```
3. Нажмите "✨ Create Flashcards"
4. Подождите пока слова переведутся

### Работа с карточками
- **Клик на карточку** - переворот и показ перевода
- **Наведите на карточку** - появится кнопка удаления (×)
- **Клик на ×** - удалить карточку

## Поддерживаемые языки

- 🇬🇧 English (en)
- 🇷🇺 Russian (ru)
- 🇪🇸 Spanish (es)
- 🇫🇷 French (fr)
- 🇩🇪 German (de)

Можно легко добавить больше языков в `app/page.tsx`

## API для перевода

Используется **MyMemory Translation API**:
- Бесплатный
- Без регистрации
- 1000 запросов в день
- Автоматическая задержка между запросами

## Структура проекта

```
wordly/
├── app/
│   ├── page.tsx          # Главная страница
│   ├── layout.tsx        # Layout приложения
│   └── globals.css       # Глобальные стили
├── components/
│   ├── Flashcard.tsx     # Компонент карточки
│   └── FlashcardGrid.tsx # Сетка карточек
├── lib/
│   ├── supabase.ts       # Клиент Supabase
│   ├── translate.ts      # API перевода
│   └── database.types.ts # TypeScript типы
├── .env.local            # Ваши API ключи (НЕ коммитить!)
└── supabase-migration.sql # SQL для создания таблицы
```

## Возможные проблемы

### "Cannot connect to Supabase"
- Проверьте `.env.local` - правильно ли скопированы ключи
- Перезапустите сервер: `Ctrl+C` и снова `npm run dev`

### "Translation failed"
- Проверьте интернет
- Возможно достигнут лимит (1000 запросов/день)
- Попробуйте через несколько секунд

### Ошибки TypeScript в редакторе
- Это нормально до установки зависимостей
- Уже исправлено командой `npm install`

## Дальнейшее развитие

Идеи для улучшения:
- 🔐 Добавить аутентификацию пользователей
- 📚 Создание наборов карточек (decks)
- 🎯 Режим тестирования/квиза
- 📊 Статистика изучения
- 🔊 Произношение слов (Text-to-Speech)
- 🌙 Темная тема (уже есть базовая поддержка)
- 📱 PWA для мобильных устройств

## Помощь

Если что-то не работает:
1. Проверьте `SETUP.md` для детальных инструкций
2. Убедитесь что все зависимости установлены: `npm install`
3. Проверьте консоль браузера на ошибки (F12)
